# Only event_relay.py (the long-running daemon) needs a dependency.
# itrs_notify.py — the script Geneos actually forks per trigger — is
# stdlib-only on purpose (see its module docstring): nothing to install on
# every Gateway host, no import-time cost added to the hot path.
httpx>=0.27

# Optional: enables HTTP/2 multiplexing to the EMS (ITRS_RELAY_HTTP2=1).
# Not required — httpx pools/reuses HTTP/1.1 keep-alive connections without
# it, which is the property this design actually depends on.
# httpx[http2]>=0.27
